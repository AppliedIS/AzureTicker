using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Security.Cryptography;
using System.IO;

namespace TickerEncryption35
{
    public class Utility
    {
        public static string GenerateSalt(int maxSize)
        {
            return string.Empty;
        }

        public static string Encrypt(string value, string password, string salt)
        {
            return string.Empty;
        }

        public static string Decrypt(string text, string password, string salt)
        {
            return string.Empty;
        }

    }
}
